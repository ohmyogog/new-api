# web-next 独立部署到 Vercel 时的推荐生产环境变量
# 推荐场景：
# - 前端部署在独立域名，如 https://app.example.com
# - 通过 vercel.json 将 /api、/v1、/mj、/pg 同域代理到目标后端

# 独立站点部署到域名根路径时必须设为 /
VITE_WEB_BASE_PATH=/

# 在 Vercel 生产环境中保持为空，前端将使用同源相对路径：
# /api/* -> vercel.json -> https://ogog.ai/api/*
# /v1/*  -> vercel.json -> https://ogog.ai/v1/*
# 不要在当前方案下填 https://ogog.ai ，否则浏览器会直接跨域请求后端
# VITE_REACT_APP_SERVER_URL=

# 仅本地 bun run dev / vite dev 调试时使用，Vercel 生产环境不会读取该值
# VITE_API_PROXY_TARGET=http://localhost:3000
